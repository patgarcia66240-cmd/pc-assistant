<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Passerelle WebSocket en réseau local uniquement : jamais d'appel sortant vers Internet,
         mais INTERNET reste nécessaire pour qu'Android autorise un ServerSocket à écouter. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Service de premier plan : garde la passerelle vivante quand l'app passe en arrière-plan
         (sinon Android coupe le socket au bout de quelques minutes). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Android 13+ : notification persistante du service de premier plan. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Capacité "contacts" (backend/plugins/android_bridge/, GET /api/android/contacts) —
         demandée à l'exécution, jamais accordée automatiquement à l'installation. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AriaPhoneBridge">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AriaPhoneBridge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".gateway.GatewayForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
