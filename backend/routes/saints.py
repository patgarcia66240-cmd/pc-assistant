# Ce fichier a été déplacé vers backend/plugins/saints/router.py le 11/09/2026, en tant que
# premier exemple du système de plugins ARIA (voir backend/plugin_loader.py). Il n'est plus
# importé par main.py. Conservé ici volontairement vide (plutôt que supprimé) pour que
# l'ancien chemin d'import échoue bruyamment s'il traîne encore quelque part, au lieu de
# faire tourner silencieusement deux copies de la même route.
